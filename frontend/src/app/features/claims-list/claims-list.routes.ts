import { Routes } from '@angular/router';
import { ClaimsListPage } from './claims-list-page';

export const CLAIMS_LIST_ROUTES: Routes = [{ path: '', component: ClaimsListPage, title: 'Claims · Claims Portal' }];
