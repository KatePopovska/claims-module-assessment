import { Routes } from '@angular/router';
import { ClaimDetailPage } from './claim-detail-page';

export const CLAIM_DETAIL_ROUTES: Routes = [{ path: '', component: ClaimDetailPage, title: 'Claim · Claims Portal' }];
